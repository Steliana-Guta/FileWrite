#include <string>
#include <iostream>
#include <fstream>
using namespace std;
/*int main(void)
{// Create object file pointer for reading
	ifstream inFile;// Define variable for data to be read from file
	string line;// Connect to file for reading
	inFile.open("testfile.txt");
	if (inFile.is_open() == false)
		cout << "Error opening file\n" << endl;
	else
	{
		while (getline(inFile, line)){cout << line;}cout << endl;
	}
	inFile.close();
	system("pause");
	return 0;
}*/